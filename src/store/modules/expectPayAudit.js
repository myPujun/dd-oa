import {api} from '../api'
import ajax from '../ajax'

const actions = {
    // getPaytList({commit},params){   //预付款列表
    //     return ajax.post(api.paymentList ,params)
    // }
}

export default {
    actions
}